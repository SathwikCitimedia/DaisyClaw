interface:
  display_name: "DaisyClaw Test Performance"
  short_description: "Benchmark tests, plugin suites, CPU, RSS, and heap growth"
  default_prompt: "Use $daisyclaw-test-performance to reassess DaisyClaw test and plugin-suite performance, collect wall/import/CPU/RSS metrics, investigate memory growth when needed, fix the next real hotspot without losing coverage, update the report, and commit scoped changes."
policy:
  allow_implicit_invocation: false
