apiVersion: v1
kind: Service
metadata:
  name: daisyclaw
  labels:
    app: daisyclaw
spec:
  type: ClusterIP
  selector:
    app: daisyclaw
  ports:
    - name: gateway
      port: 18789
      targetPort: 18789
      protocol: TCP
